---
phase: 06-content-sections
plan: 04
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/locales/en/stack.json
  - src/locales/es/stack.json
  - src/components/stack/StackClaude.tsx
  - src/components/stack/StackN8n.tsx
  - src/components/stack/StackMake.tsx
  - src/components/stack/stack-registry.ts
  - src/components/stack/StackIcon.tsx
  - src/components/sections/Stack.tsx
  # Contingency files — only created if @icons-pack/react-simple-icons v13 is missing
  # SiRailway and/or SiApify (verify at execute-time, fall back to inline TSX from
  # @lobehub/icons-static-svg sources following the same pattern as StackClaude/N8n/Make):
  - public/icons/railway.svg
  - public/icons/apify.svg
  - src/components/stack/StackRailway.tsx
  - src/components/stack/StackApify.tsx
autonomous: true

must_haves:
  truths:
    - "Stack section displays all 13 tools (Make, n8n, Claude/MCP, Supabase, Python, JavaScript, Docker, Railway, Google Cloud, GitHub, Airtable, Notion, Apify) as icon + name pairs"
    - "Tools are grouped into 5 categories (Automation / AI / Infrastructure / Languages / Data)"
    - "Section displays the dot-grid radial-gradient background per DESIGN-06"
    - "Hovering a tool scales it by ~1.08x (CSS transition, reduced-motion respected)"
    - "Section title translates EN/ES (EN: Stack, ES: Herramientas); category labels translate EN/ES; tool brand names stay in EN"
    - "Section is reachable via the #stack nav anchor"
  artifacts:
    - path: "src/components/stack/stack-registry.ts"
      provides: "Single source of truth for the 13 tools + their categories + icon dispatch"
      exports: ["STACK", "StackCategory", "StackEntry"]
    - path: "src/components/stack/StackIcon.tsx"
      provides: "Dispatcher component: React component icon vs inline SVG component"
      exports: ["StackIcon"]
    - path: "src/components/stack/StackClaude.tsx"
      provides: "Inline SVG component for Claude (not in Simple Icons)"
      exports: ["StackClaude"]
    - path: "src/components/stack/StackN8n.tsx"
      provides: "Inline SVG component for n8n (not in Simple Icons)"
      exports: ["StackN8n"]
    - path: "src/components/stack/StackMake.tsx"
      provides: "Inline SVG component for Make.com (not in Simple Icons)"
      exports: ["StackMake"]
    - path: "src/components/sections/Stack.tsx"
      provides: "<Stack /> section component"
      exports: ["Stack"]
      min_lines: 30
    - path: "src/locales/en/stack.json"
      provides: "EN stack title + category labels"
      contains: "categories"
    - path: "src/locales/es/stack.json"
      provides: "ES stack title + category labels"
      contains: "categories"
  key_links:
    - from: "src/components/stack/stack-registry.ts"
      to: "@icons-pack/react-simple-icons"
      via: "named imports for 10 brand-icon React components"
      pattern: "@icons-pack/react-simple-icons"
    - from: "src/components/sections/Stack.tsx"
      to: "src/components/stack/stack-registry.ts"
      via: "STACK constant + ORDER iteration"
      pattern: "stack-registry"
    - from: "src/components/sections/Stack.tsx"
      to: "src/components/stack/StackIcon.tsx"
      via: "StackIcon dispatch per entry"
      pattern: "StackIcon"
    - from: "src/components/stack/StackClaude.tsx, StackN8n.tsx, StackMake.tsx"
      to: "public/icons/{claude,n8n,make}.svg"
      via: "SVG markup transcribed into TSX components"
      pattern: "currentColor"
---

<objective>
Ship the Stack section: a categorized 13-tool icon grid (5 categories) over a dot-grid radial-gradient background. Ten icons come from `@icons-pack/react-simple-icons`; three (Claude, n8n, Make.com) ship as inline TSX components built from the SVGs copied into `public/icons/` in Plan 06-01.

Purpose: Satisfies ROADMAP Phase 6 success criterion 3 (categorized visual grid of all 13 listed tools over a dot-grid background) plus DESIGN-06 (dot-grid radial-gradient pattern). Honors CONTEXT.md locked decisions (icon + name only, no descriptors, hover scale 1.05-1.1x, tool names stay in EN, section title + category labels translate).
Output: Two locale JSON files, three inline-SVG TSX components for the missing brands, a registry module, a dispatcher icon component, and the Stack section component itself.
</objective>

<execution_context>
@C:/Users/KLBVT/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KLBVT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-sections/06-CONTEXT.md
@.planning/phases/06-content-sections/06-RESEARCH.md
@src/components/scroll/RevealSection.tsx
@src/locales/en/stack.json
@src/locales/es/stack.json
@src/@types/i18next.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stack.json locales + create 3 inline-SVG TSX components for Claude/n8n/Make</name>
  <files>src/locales/en/stack.json, src/locales/es/stack.json, src/components/stack/StackClaude.tsx, src/components/stack/StackN8n.tsx, src/components/stack/StackMake.tsx</files>
  <action>
Two parts:

PART A — Update locale JSON files. Replace the `{ "_placeholder": "..." }` stub in BOTH files with:

```json
{
  "title": "Stack",
  "categories": {
    "automation": "Automation",
    "ai": "AI",
    "infrastructure": "Infrastructure",
    "languages": "Languages",
    "data": "Data"
  }
}
```

ES translations:
- title: "Herramientas" (translates per locked CONTEXT.md decision: "section title and category labels should translate"; the section heading is distinct from the PillNav/MobileNav nav link, which stays "Stack" — keeping the nav label brand-fixed while the h2 heading translates is the natural Spanish read for the heading itself)
- categories: "Automatización" / "IA" / "Infraestructura" / "Lenguajes" / "Datos"

NO tool brand names in the JSON — they live in `stack-registry.ts` as static strings (locked: tool brand names never translate).

PART B — Create the three inline-SVG TSX components. Read each source SVG with the Read tool first:
- Source: `public/icons/claude.svg` → `src/components/stack/StackClaude.tsx`
- Source: `public/icons/n8n.svg` → `src/components/stack/StackN8n.tsx`
- Source: `public/icons/make.svg` → `src/components/stack/StackMake.tsx`

Each TSX component follows this exact shape (substitute `<path>` from the source SVG):

```tsx
interface IconProps {
  size?: number
  color?: string
  className?: string
}

export function StackClaude({ size = 32, color = 'currentColor', className }: IconProps) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill={color}
      className={className}
      aria-hidden="true"
    >
      {/* paste the original <path> / <g> children from public/icons/claude.svg here */}
    </svg>
  )
}
```

Rules:
- Match the size + color + className interface so `<StackIcon entry={...}>` can dispatch uniformly with the 10 `@icons-pack/react-simple-icons` components (which expose `size`, `color`, `className` with the same defaults).
- `color` defaults to `'currentColor'` so the icon inherits CSS `color` — solves research Pitfall 2 (SVG-via-img loses color). Inlining the SVG into a `.tsx` is the recommended fix.
- `aria-hidden="true"` is mandatory — the accessible name is the sibling `<span>` text label in `Stack.tsx`.
- Preserve the original `viewBox` from the source SVG (likely `0 0 24 24` for all three).
- If the source SVG uses `fill="currentColor"` on inner elements, leave those as-is — the outer `fill={color}` cascades correctly.
- DO NOT import the raw SVG via `?raw` + `dangerouslySetInnerHTML`. The locked decision in research Open Question 5 was: inline as TSX for a uniform React-component interface across all 13 icons.
- DO NOT use `<img src="/icons/claude.svg" />` — research Pitfall 2 explains why (CSS color cannot reach the SVG document, icon renders solid black on dark bg).
  </action>
  <verify>
`cat src/locales/en/stack.json src/locales/es/stack.json` shows both files with `title` and a `categories` object containing all 5 category keys. `grep -c "_placeholder" src/locales/{en,es}/stack.json` returns 0 for both. `ls src/components/stack/Stack{Claude,N8n,Make}.tsx` succeeds. Each component exports a named function, is `aria-hidden="true"`, uses `fill={color}` with `color = 'currentColor'` default. `npm run build` and `npm run lint` succeed.
  </verify>
  <done>Stack locales contain the title + 5-category labels in both EN and ES. Three TSX icon components exist with the uniform `{ size, color, className }` interface and inline `<svg>` markup from the source SVGs.</done>
</task>

<task type="auto">
  <name>Task 2: Create stack-registry.ts + StackIcon.tsx dispatcher (with SiRailway / SiApify fallback plan)</name>
  <files>src/components/stack/stack-registry.ts, src/components/stack/StackIcon.tsx</files>
  <action>
PART A — Create `src/components/stack/stack-registry.ts`:

```typescript
import type { ComponentType } from 'react'
import {
  SiAirtable, SiSupabase, SiApify, SiPython, SiJavascript,
  SiDocker, SiNotion, SiGithub, SiGooglecloud, SiRailway,
} from '@icons-pack/react-simple-icons'
import { StackClaude } from './StackClaude'
import { StackN8n } from './StackN8n'
import { StackMake } from './StackMake'

export type StackCategory = 'automation' | 'ai' | 'infrastructure' | 'languages' | 'data'

interface IconComponentProps {
  size?: number
  color?: string
  className?: string
}

export interface StackEntry {
  id: string
  name: string // brand-fixed, never translated
  category: StackCategory
  Icon: ComponentType<IconComponentProps>
}

export const STACK: readonly StackEntry[] = [
  // Automation
  { id: 'make',     name: 'Make.com',     category: 'automation',    Icon: StackMake },
  { id: 'n8n',      name: 'n8n',          category: 'automation',    Icon: StackN8n },
  // AI
  { id: 'claude',   name: 'Claude / MCP', category: 'ai',            Icon: StackClaude },
  // Infrastructure
  { id: 'docker',   name: 'Docker',       category: 'infrastructure', Icon: SiDocker },
  { id: 'railway',  name: 'Railway',      category: 'infrastructure', Icon: SiRailway },
  { id: 'gcp',      name: 'Google Cloud', category: 'infrastructure', Icon: SiGooglecloud },
  { id: 'github',   name: 'GitHub',       category: 'infrastructure', Icon: SiGithub },
  // Languages
  { id: 'python',   name: 'Python',       category: 'languages',     Icon: SiPython },
  { id: 'js',       name: 'JavaScript',   category: 'languages',     Icon: SiJavascript },
  // Data
  { id: 'supabase', name: 'Supabase',     category: 'data',          Icon: SiSupabase },
  { id: 'airtable', name: 'Airtable',     category: 'data',          Icon: SiAirtable },
  { id: 'notion',   name: 'Notion',       category: 'data',          Icon: SiNotion },
  { id: 'apify',    name: 'Apify',        category: 'data',          Icon: SiApify },
] as const

export const STACK_ORDER: readonly StackCategory[] = ['automation', 'ai', 'infrastructure', 'languages', 'data'] as const
```

Critical rules:
- DO NOT import `SiN8n`, `SiMake`, or `SiClaude` from `@icons-pack/react-simple-icons` — research Pitfall 1 documents that these do NOT exist in Simple Icons. Use `StackN8n` / `StackMake` / `StackClaude` from the inline TSX components.

- BEFORE writing this file, verify that BOTH `SiRailway` and `SiApify` exist in the installed `@icons-pack/react-simple-icons` v13.x:
  - Quick check: `node -e "const m = require('@icons-pack/react-simple-icons'); console.log({railway: !!m.SiRailway, apify: !!m.SiApify})"` should print `{ railway: true, apify: true }`.
  - Alternative: `grep -E "Si(Railway|Apify)" node_modules/@icons-pack/react-simple-icons/dist/index.d.ts` should return hits for both.

- **SiRailway fallback plan (research-flagged ambiguity):** If the import errors at build time OR the precheck above returns `railway: false`, treat Railway exactly the same way as Claude/n8n/Make in Task 1:
  1. Fetch `railway.svg` from `@lobehub/icons-static-svg` (same pattern as Plan 06-01 did for the inline-set) and place at `public/icons/railway.svg`.
  2. Create `src/components/stack/StackRailway.tsx` with the same `{ size, color, className }` interface and `fill={color}` default `'currentColor'`.
  3. Replace the `SiRailway` import here with `import { StackRailway } from './StackRailway'` and the STACK entry's `Icon: SiRailway` with `Icon: StackRailway`.
  4. Note this in the plan summary.

- **SiApify fallback plan (mirror of SiRailway):** If the import errors at build time OR the precheck returns `apify: false`, apply the IDENTICAL recovery path used for Railway above:
  1. Fetch `apify.svg` from `@lobehub/icons-static-svg` and place at `public/icons/apify.svg`.
  2. Create `src/components/stack/StackApify.tsx` with the same `{ size, color, className }` interface and `fill={color}` default `'currentColor'`.
  3. Replace the `SiApify` import here with `import { StackApify } from './StackApify'` and the STACK entry's `Icon: SiApify` with `Icon: StackApify`.
  4. Note this in the plan summary.
- The reason both Railway and Apify need explicit fallback plans: both are relatively recent additions to Simple Icons. Module-export names occasionally lag behind slug additions across `@icons-pack/react-simple-icons` minor versions. Treating them symmetrically prevents a one-line build failure with no documented recovery.

- Five categories (NOT four) per research Open Question 2 — Python and JS get their own "Languages" bucket. The user's "or equivalent grouping" latitude was explicit.
- `STACK_ORDER` defines the render order of categories — Automation first (Kelly's primary differentiator), AI second, then infra/languages/data.

PART B — Create `src/components/stack/StackIcon.tsx`:

```tsx
import type { StackEntry } from './stack-registry'

interface StackIconProps {
  entry: StackEntry
  size?: number
}

export function StackIcon({ entry, size = 36 }: StackIconProps) {
  const { Icon } = entry
  return <Icon size={size} color="currentColor" />
}
```

Tiny dispatcher — the registry already unifies the interface so this is a one-line render. Reason for separating into its own file: future flexibility (e.g. adding fallback rendering for missing icons) without touching the registry.
  </action>
  <verify>
Precheck first: `node -e "const m = require('@icons-pack/react-simple-icons'); console.log(JSON.stringify({railway: !!m.SiRailway, apify: !!m.SiApify}))"` — both must print `true`. If either prints `false`, execute the matching fallback (StackRailway.tsx and/or StackApify.tsx) per the action block BEFORE proceeding.

Then: `ls src/components/stack/stack-registry.ts src/components/stack/StackIcon.tsx` succeeds. `npm run build` succeeds — TypeScript must resolve all 10 (or fewer, if fallbacks applied) named imports from `@icons-pack/react-simple-icons` AND all 3 (or 4 / 5, if fallbacks applied) inline TSX component imports. `npm run lint` is clean. `STACK.length === 13` and `STACK_ORDER.length === 5`. Each category has at least one entry. If `StackRailway.tsx` was created, `ls src/components/stack/StackRailway.tsx` succeeds and `public/icons/railway.svg` exists. If `StackApify.tsx` was created, `ls src/components/stack/StackApify.tsx` succeeds and `public/icons/apify.svg` exists.
  </verify>
  <done>Registry exports STACK (13 entries with id/name/category/Icon), StackCategory type, STACK_ORDER (5-category render order). StackIcon dispatcher renders the entry's icon at `currentColor`. If SiRailway or SiApify were unavailable, the matching fallback `Stack{Railway,Apify}.tsx` inline component exists and the registry imports from it instead of `@icons-pack/react-simple-icons`.</done>
</task>

<task type="auto">
  <name>Task 3: Create Stack.tsx section with dot-grid background + categorized icon grid + hover scale</name>
  <files>src/components/sections/Stack.tsx</files>
  <action>
Create `src/components/sections/Stack.tsx` exporting `Stack`. Follows Pattern 4 in 06-RESEARCH.md.

Behavior:
- `id="stack"` (matches `common.nav.stack`).
- Section padding `6rem 1.5rem`.
- Dot-grid radial-gradient background (per DESIGN-06, locked decision and research Open Question 1 settled on Stack-only):
  ```css
  background-image: radial-gradient(circle, #333 1px, transparent 1px);
  background-size: 40px 40px;
  ```
  Apply via inline `style` to the `<section>` so it stays self-contained (no global CSS edit).
- Heading `<h2>` with `font-display`, large size, from `t('title')`.
- Iterate `STACK_ORDER` to render one category block per category:
  ```tsx
  {STACK_ORDER.map((cat) => (
    <div key={cat}>
      <h3>{t(`categories.${cat}`)}</h3>
      <ul>
        {STACK.filter((s) => s.category === cat).map((entry) => (
          <li key={entry.id} className="stack-tile">
            <StackIcon entry={entry} />
            <span>{entry.name}</span>
          </li>
        ))}
      </ul>
    </div>
  ))}
  ```
- `<ul>` is a CSS Grid: `gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))'`, gap 1.5rem, list-style none, padding 0.
- Each `<li>` (`.stack-tile`):
  - Flex column, items centered, gap 0.5rem, padding 1rem.
  - Hover scale 1.08 via CSS — apply with a small `<style>` block in the same file OR an inline approach via `onMouseEnter` (CSS is simpler). Use a CSS-in-React pattern: declare a `<style>` tag scoped to the section (small CSS string for `.stack-tile { transition: transform 0.2s ease } .stack-tile:hover { transform: scale(1.08) } @media (prefers-reduced-motion: reduce) { .stack-tile { transition: none } .stack-tile:hover { transform: none } }`).
  - This satisfies the "respects reduced-motion policy" locked decision — reduced-motion users get no transform on hover.
- `<span>` label uses `entry.name` directly (NOT translated) — locked decision: tool brand names stay in EN.
- Section title `<h2>` and category `<h3>` labels translate via `t('title')` and `t('categories.${cat}')`.

Wrap the contents inside the `<section>` in a single `<RevealSection>` with `stagger={false}` (single fade-up for the whole content block — the per-tile hover scale handles interactivity). Per research Pitfall 5: do NOT put a `<RevealSection stagger>` over a wrapper `<div>` that wraps the category blocks — stagger only fires when its direct children are the elements to stagger; the category blocks have varying structure and DON'T need stagger.

DO NOT add a dot-grid mask fade unless verification reveals a hard edge. Keep the gradient simple per locked decision (`background-image: radial-gradient(circle, #333 1px, transparent 1px); background-size: 40px 40px;`).
  </action>
  <verify>
`ls src/components/sections/Stack.tsx` succeeds. `npm run build` and `npm run lint` succeed. `grep 'id="stack"' src/components/sections/Stack.tsx` returns a hit. `grep "STACK_ORDER" src/components/sections/Stack.tsx` returns a hit. `grep "radial-gradient" src/components/sections/Stack.tsx` returns a hit. In a dev server: scrolling to `#stack` shows the dot-grid background; all 13 tools render across 5 category blocks; hovering any tile scales it ~1.08x; toggling EN/ES changes the section heading and category headings but tool names stay in EN.
  </verify>
  <done>Stack.tsx renders all 13 tools across 5 categories over a radial-gradient dot-grid background. Hover scale is CSS-driven and reduced-motion-safe. Section title + category labels translate; tool names stay in EN.</done>
</task>

</tasks>

<verification>
- `src/locales/en/stack.json` and `src/locales/es/stack.json` both contain `title` and `categories.{automation,ai,infrastructure,languages,data}`
- `grep "_placeholder" src/locales/{en,es}/stack.json` returns no hits
- `src/components/stack/{StackClaude,StackN8n,StackMake}.tsx` exist with inline `<svg>` markup
- `src/components/stack/stack-registry.ts` exports `STACK` (length 13), `StackCategory` type, `STACK_ORDER` (length 5)
- `src/components/stack/StackIcon.tsx` exports `StackIcon`
- `src/components/sections/Stack.tsx` exports `Stack`, renders `<section id="stack">` with `radial-gradient` background
- `npm run build` and `npm run lint` succeed
- No imports of `SiN8n`, `SiMake`, `SiClaude` from `@icons-pack/react-simple-icons`
- If `SiRailway` or `SiApify` were unavailable in v13.x, the matching fallback `Stack{Railway,Apify}.tsx` + `public/icons/{railway,apify}.svg` exist
</verification>

<success_criteria>
1. All 13 tools listed in CONTEXT.md render with brand-accurate icons.
2. Tools group into 5 visible categories with translated labels.
3. Dot-grid radial-gradient background renders behind the section (DESIGN-06).
4. Tile hover scales ~1.08x via CSS transition (gracefully degraded for reduced-motion).
5. Tool brand names stay in EN regardless of active language; section heading + category labels switch on EN/ES toggle.
6. Section is reachable via `#stack` nav anchor.
7. `npm run build` and `npm run lint` both succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-sections/06-04-SUMMARY.md`.
</output>

---
phase: 06-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - public/Harvard_CV_Kelly_Battistoni_EN.pdf
  - public/Harvard_CV_Kelly_Battistoni_ES.pdf
  - public/icons/claude.svg
  - public/icons/n8n.svg
  - public/icons/make.svg
autonomous: true

must_haves:
  truths:
    - "Stack components can import brand icons from a tree-shaken React package"
    - "The three missing brand SVGs (Claude, n8n, Make.com) are reachable as static assets"
    - "Both CV PDFs are publicly downloadable via /Harvard_CV_Kelly_Battistoni_{EN,ES}.pdf"
  artifacts:
    - path: "package.json"
      provides: "@icons-pack/react-simple-icons dep declaration"
      contains: "@icons-pack/react-simple-icons"
    - path: "public/Harvard_CV_Kelly_Battistoni_EN.pdf"
      provides: "EN CV download target"
    - path: "public/Harvard_CV_Kelly_Battistoni_ES.pdf"
      provides: "ES CV download target"
    - path: "public/icons/claude.svg"
      provides: "Claude brand SVG source for StackClaude.tsx"
    - path: "public/icons/n8n.svg"
      provides: "n8n brand SVG source for StackN8n.tsx"
    - path: "public/icons/make.svg"
      provides: "Make.com brand SVG source for StackMake.tsx"
  key_links:
    - from: "package.json"
      to: "node_modules/@icons-pack/react-simple-icons"
      via: "npm install"
      pattern: "@icons-pack/react-simple-icons"
---

<objective>
Lay the Phase 6 foundation: install the single new dependency for brand icons and copy the static assets (CV PDFs + three missing brand SVGs) into `public/` so all four content-section plans can reference them in parallel.

Purpose: Unblock the four parallel section plans (About, Projects, Stack, Contact). Centralizing all "asset + dep" work in one tiny plan means the section plans never race for `package.json` or `public/` writes.
Output: One new npm dependency installed, five new files in `public/`.
</objective>

<execution_context>
@C:/Users/KLBVT/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KLBVT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-sections/06-CONTEXT.md
@.planning/phases/06-content-sections/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @icons-pack/react-simple-icons</name>
  <files>package.json, package-lock.json</files>
  <action>
Run `npm install @icons-pack/react-simple-icons` to add the brand-icon dependency.

Why this package (per 06-RESEARCH.md):
- React 19 peer (^16.13 || ^17 || ^18 || ^19), MIT, `sideEffects: false` → tree-shakable
- Covers 10 of the 13 stack tools (Airtable, Supabase, Apify, Python, JavaScript, Docker, Notion, GitHub, Google Cloud, Anthropic)
- DO NOT use `react-icons/si` (historical tree-shaking gaps) or `@lobehub/icons` (hard peer deps on `@lobehub/ui` + `antd`)

Verify the installed major version is 13.x (matches the version planned against). The other three tools (Claude / MCP, n8n, Make.com) are NOT in Simple Icons — they ship as static SVGs in Task 2; do NOT try to import `SiClaude`, `SiN8n`, or `SiMake` from this package (they do not exist).
  </action>
  <verify>
`grep "@icons-pack/react-simple-icons" package.json` returns a `dependencies` line. `npm ls @icons-pack/react-simple-icons` reports a 13.x version with no peer-dep warnings. `npm run build` still completes without errors.
  </verify>
  <done>Dependency declared in `package.json`, present in `node_modules/`, no peer-dep warnings, build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Copy CV PDFs and brand SVG assets into public/</name>
  <files>public/Harvard_CV_Kelly_Battistoni_EN.pdf, public/Harvard_CV_Kelly_Battistoni_ES.pdf, public/icons/claude.svg, public/icons/n8n.svg, public/icons/make.svg</files>
  <action>
Two filesystem moves, one fetch:

1. Copy the two CV PDFs already present in the repo root into `public/`:
   - `Harvard_CV_Kelly_Battistoni_EN.pdf` → `public/Harvard_CV_Kelly_Battistoni_EN.pdf`
   - `Harvard_CV_Kelly_Battistoni_ES.pdf` → `public/Harvard_CV_Kelly_Battistoni_ES.pdf`
   Use `cp` (POSIX) or `Copy-Item` (PowerShell). DO NOT delete the originals from the repo root — leave them in place (the user keeps them there for reference). The `public/` copies are what Vite serves at `/Harvard_CV_Kelly_Battistoni_EN.pdf` etc.

2. Create the `public/icons/` directory if it does not already exist.

3. Fetch the three missing brand SVGs from `@lobehub/icons-static-svg` via unpkg and save them under `public/icons/`:
   - `https://unpkg.com/@lobehub/icons-static-svg@latest/icons/claude.svg` → `public/icons/claude.svg`
   - `https://unpkg.com/@lobehub/icons-static-svg@latest/icons/n8n.svg` → `public/icons/n8n.svg`
   - `https://unpkg.com/@lobehub/icons-static-svg@latest/icons/make.svg` → `public/icons/make.svg`
   Use `curl -L -o <path> <url>` or `Invoke-WebRequest -OutFile`. Each SVG should be 24×24 viewBox with `fill="currentColor"` (the SVGs will be re-wrapped as inline TSX components in Plan 06-04 — these raw files are the source).

DO NOT install `@lobehub/icons-static-svg` as an npm package (would add a ~900-icon package for 3 files — locked decision). DO NOT preload PDFs via `<link rel="prefetch">` in `index.html` — they are click-driven downloads (avoids Pitfall 8 from research).
  </action>
  <verify>
All five files exist at the expected paths: `ls public/Harvard_CV_*.pdf public/icons/*.svg` lists exactly 2 PDFs and 3 SVGs. Each SVG file's first 200 bytes contain `viewBox="0 0 24 24"` and `fill="currentColor"` (or equivalent — confirm SVGs are not empty and not HTML 404 pages). Run `npm run dev` and open `http://localhost:5173/Harvard_CV_Kelly_Battistoni_EN.pdf` — the browser downloads or previews the PDF (NOT a 404).
  </verify>
  <done>2 PDFs in `public/`, 3 SVGs in `public/icons/`, each one a valid file (not a 404 HTML body), Vite serves each at its `/`-prefixed URL.</done>
</task>

</tasks>

<verification>
- `package.json` declares `@icons-pack/react-simple-icons` in `dependencies`
- `npm ls @icons-pack/react-simple-icons` reports a 13.x version
- `ls public/Harvard_CV_Kelly_Battistoni_EN.pdf public/Harvard_CV_Kelly_Battistoni_ES.pdf` succeeds
- `ls public/icons/claude.svg public/icons/n8n.svg public/icons/make.svg` succeeds
- Each SVG is a real SVG (not a 404 HTML body) — `grep -l "<svg" public/icons/*.svg` returns all three
- `npm run build` succeeds
</verification>

<success_criteria>
1. Single new npm dependency `@icons-pack/react-simple-icons` (13.x) installed, no peer-dep warnings.
2. Both CV PDFs live in `public/` and are reachable at their root URLs via the Vite dev server.
3. Three brand SVGs (Claude, n8n, Make) live in `public/icons/` with `fill="currentColor"` markup.
4. `npm run build` and `npm run dev` both still succeed after the changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-sections/06-01-SUMMARY.md`.
</output>

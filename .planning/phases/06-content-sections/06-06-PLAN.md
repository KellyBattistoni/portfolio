---
phase: 06-content-sections
plan: 06
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
  - "06-04"
  - "06-05"
files_modified:
  - src/App.tsx
autonomous: false

must_haves:
  truths:
    - "Visiting / shows Hero → About → Projects → Stack → Contact in that order"
    - "Each section is reachable by clicking the corresponding PillNav/MobileNav link"
    - "Each section is wrapped in its own AnimationErrorBoundary (failure in one doesn't kill the others)"
    - "All four content sections are bilingual via the existing EN/ES toggle"
    - "ScrollTrigger anchors stay correct after font load (existing document.fonts.ready hook still fires)"
    - "The Phase 5 placeholder div (200vh background) is removed"
    - "All 4 ROADMAP Phase 6 success criteria pass user browser verification"
  artifacts:
    - path: "src/App.tsx"
      provides: "Top-level layout wiring all 4 new sections into the page after Hero+Nav"
      contains: "About"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/sections/About.tsx"
      via: "<AnimationErrorBoundary><About /></AnimationErrorBoundary>"
      pattern: "<About"
    - from: "src/App.tsx"
      to: "src/components/sections/Projects.tsx"
      via: "<AnimationErrorBoundary><Projects /></AnimationErrorBoundary>"
      pattern: "<Projects"
    - from: "src/App.tsx"
      to: "src/components/sections/Stack.tsx"
      via: "<AnimationErrorBoundary><Stack /></AnimationErrorBoundary>"
      pattern: "<Stack"
    - from: "src/App.tsx"
      to: "src/components/sections/Contact.tsx"
      via: "<AnimationErrorBoundary><Contact /></AnimationErrorBoundary>"
      pattern: "<Contact"
---

<objective>
Wire the four new content sections into `App.tsx` (replacing the Phase 5 placeholder div) and run a human browser verification checkpoint against all four ROADMAP Phase 6 success criteria + the locked CONTEXT.md decisions (NDA-safe copy, dot-grid background, CV download independence).

Purpose: Closes Phase 6. The implementation is trivially small (one App.tsx edit) but the verification step is load-bearing — it covers the copy iteration loop (user refines About + Projects + Contact prose in their own voice), confirms the dot-grid renders cleanly, exercises the CV language toggle, and audits the LinkedIn placeholder (flagged for Phase 7 swap).
Output: Modified `App.tsx` + user-approved phase completion.
</objective>

<execution_context>
@C:/Users/KLBVT/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/KLBVT/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-sections/06-CONTEXT.md
@.planning/phases/06-content-sections/06-RESEARCH.md
@src/App.tsx
@src/components/error/AnimationErrorBoundary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire About + Projects + Stack + Contact into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Modify `src/App.tsx` to replace the Phase 5 placeholder `<div>` (currently 200vh dark background) with the four new section components, each wrapped in its own `AnimationErrorBoundary`.

Imports to ADD at the top:

```tsx
import { About } from '@/components/sections/About'
import { Projects } from '@/components/sections/Projects'
import { Stack } from '@/components/sections/Stack'
import { Contact } from '@/components/sections/Contact'
import { AnimationErrorBoundary } from '@/components/error/AnimationErrorBoundary'
```

Replace the existing `{/* Phase 6 placeholder ... */}` line + the `<div style={{ height: '200vh', ... }} />` immediately after with:

```tsx
<AnimationErrorBoundary><About /></AnimationErrorBoundary>
<AnimationErrorBoundary><Projects /></AnimationErrorBoundary>
<AnimationErrorBoundary><Stack /></AnimationErrorBoundary>
<AnimationErrorBoundary><Contact /></AnimationErrorBoundary>
```

Order MATTERS:
- Hero comes first (already in place).
- PillNav / MobileNav second (already in place, scroll-triggered to appear).
- About → Projects → Stack → Contact in that exact order — matches the nav link order in `common.nav` (`about` → `work` → `stack` → `contact`).

Why ONE error boundary per section (not one wrapping all four): research Pattern 6 + locked decision from STATE [02-02]. A crash in `Projects` (parallax bug) should NOT take About / Stack / Contact down with it. Isolated crash domains preserve usability.

Leave everything else in App.tsx intact:
- `NoiseOverlay` stays first child (full-page overlay).
- `Hero sectionRef={heroRef}` unchanged.
- `isMobile ? <MobileNav heroRef={heroRef} /> : <PillNav heroRef={heroRef} />` unchanged.
- The `useEffect` that calls `ScrollTrigger.refresh()` after `document.fonts.ready` stays — newly added sections introduce more text, font-swap-induced layout shift would invalidate the new sections' ScrollTrigger anchors without this refresh.

DO NOT remove or modify `NoiseOverlay`, `Hero`, `PillNav`, `MobileNav`, the `useLocalizeDocumentAttributes()` call, or the `ScrollTrigger.refresh()` effect.
DO NOT add a `<main>` wrapper around the four sections (each `<section>` already provides the landmark; adding `<main>` would conflict with the existing layout).
DO NOT add a `<footer>` — Contact IS the final section by design.
  </action>
  <verify>
`grep "About" src/App.tsx`, `grep "Projects" src/App.tsx`, `grep "Stack" src/App.tsx`, `grep "Contact" src/App.tsx` all return hits. `grep "200vh" src/App.tsx` returns NO hits (placeholder div removed). `grep -c "AnimationErrorBoundary" src/App.tsx` returns 5 (1 import + 4 wrapper usages). `npm run build` succeeds. `npm run lint` is clean. `npm run dev` boots successfully. In a browser at `http://localhost:5173/`: Hero is the first viewport, scrolling reveals About → Projects → Stack → Contact in order. Clicking PillNav links navigates between sections.
  </verify>
  <done>App.tsx renders the full page: NoiseOverlay → Hero → Nav → About → Projects → Stack → Contact. Each new section is in its own AnimationErrorBoundary. Phase 5 placeholder div is gone. `npm run build`, `npm run lint`, and `npm run dev` all succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification of all 4 Phase 6 success criteria</name>
  <files></files>
  <action>Boot the dev server (`npm run dev`), open the printed URL, and step through every check in the how-to-verify block below. Reply with "approved" once all four ROADMAP Phase 6 success criteria pass and the copy iteration loop for About / Projects / Contact is complete. Describe any failure precisely so the executor can re-run verification after fixes.</action>
  <verify>User has typed "approved" after confirming all four ROADMAP Phase 6 success criteria pass in a real browser AND the copy iteration loop on About / Projects / Contact has been completed (or explicitly deferred).</verify>
  <done>All 4 ROADMAP Phase 6 success criteria confirmed passing by the user. Draft copy reviewed in both EN and ES. CV download confirmed serving correct PDF per local language picker, independent of site language. LINKEDIN_URL placeholder acknowledged and flagged for Phase 7.</done>
  <what-built>
Phase 6 ships four bilingual content sections wired into App.tsx after Hero/Nav:
- About: 2-3 paragraphs of prose with a brand-accent divider, reveals on scroll, no bullet points, no headers between paragraphs.
- Projects: 4 case-study cards in an alternating left/right desktop grid, each with a distinct parallax speed (0.10/0.15/0.20/0.25), each expandable in place to reveal Problem/Solution/Result. Tech tags as pills.
- Stack: 13 tools across 5 categories (Automation / AI / Infrastructure / Languages / Data) over a radial-gradient dot-grid background. Tile hover scales 1.08x.
- Contact: heading + invite + mailto:kelly@seomarketing.com + LinkedIn (placeholder LINKEDIN_URL) + CV download with explicit EN/ES toggle independent of site language.
All copy is draft — the verification step is BOTH a correctness check AND a copy-iteration loop.
  </what-built>
  <how-to-verify>
1. Boot the dev server: `npm run dev`. Open `http://localhost:5173/` in a desktop browser.

ROADMAP Phase 6 Success Criterion 1 — About:
 a. Scroll past Hero into About. Read all paragraphs.
 b. Confirm there are NO bullet points and NO headers between paragraphs.
 c. Confirm the brand-accent (orange) divider line above the heading is visible.
 d. Confirm the About paragraphs stagger-fade on scroll into view.
 e. Toggle to ES via the PillNav switcher. Confirm About re-renders in Spanish with the same structure (no untranslated keys like `about:title` appear).
 f. Read both EN and ES About copy aloud. Edit `src/locales/en/about.json` and `src/locales/es/about.json` directly if the voice needs adjustment — this is the iteration loop the user opted into.

ROADMAP Phase 6 Success Criterion 2 — Projects:
 a. Scroll into Projects. Confirm 4 cards render, alternating left and right at desktop widths.
 b. Confirm each card has a visible project title, context line, tech-tag pills, and an orange outcome line.
 c. Scroll slowly through the section. Confirm each card's content drifts at a distinct speed (depth perception — the speeds should not look identical).
 d. Click any card. Confirm the Problem/Solution/Result panel expands in place with a smooth height transition. Click again. Confirm it collapses.
 e. Tab to a card with the keyboard. Press Enter or Space. Confirm the panel expands (button + aria-expanded works).
 f. Toggle to ES. Confirm all 4 cards' titles, context, outcome, problem, solution, result re-render in Spanish. Tech tags STAY in EN (brand-fixed — Make.com, n8n, etc.).
 g. Read all 4 cards' draft copy in both EN and ES. Edit `src/locales/en/projects.json` and `src/locales/es/projects.json` directly to refine — this is the documented collaborative draft loop. Confirm NDA safety: no org names, no proprietary internals.

ROADMAP Phase 6 Success Criterion 3 — Stack:
 a. Scroll into Stack. Confirm the dot-grid radial-gradient background is visible (small grey dots on dark bg, 40px spacing).
 b. Count the tools: 13 total (Make.com, n8n, Claude/MCP, Supabase, Python, JavaScript, Docker, Railway, Google Cloud, GitHub, Airtable, Notion, Apify).
 c. Confirm 5 category headings render (Automation / AI / Infrastructure / Languages / Data — or their ES equivalents).
 d. Confirm each icon renders with brand color (NOT a black blob — research Pitfall 2 sanity check).
 e. Hover any tile. Confirm a subtle scale-up (~1.08x).
 f. Toggle to ES. Confirm section title changes to "Herramientas" (ES translation per Plan 06-04) AND tool names stay in EN. Category headings translate (Automatización / IA / Infraestructura / Lenguajes / Datos).
 g. (Optional) Open browser DevTools → Settings → Rendering → Emulate "prefers-reduced-motion: reduce". Confirm hover scale is disabled.

ROADMAP Phase 6 Success Criterion 4 — Contact + CV:
 a. Scroll into Contact (the final section). Confirm heading + invite copy render.
 b. Click the email link. Confirm the OS default mail client opens with `kelly@seomarketing.com` pre-filled.
 c. Confirm the LinkedIn label renders as a muted, non-clickable span (NOT a clickable link). This is the expected behavior while the `LINKEDIN_URL` placeholder is active — the conditional rendering in Plan 06-05 deliberately prevents a broken `target="_blank"` anchor. Note this for Phase 7 swap.
 d. Confirm the CV picker shows two buttons "EN" and "ES" with aria-checked states. Click each. Confirm only one is checked at a time.
 e. Click "EN" picker, then click "Download". Confirm `Harvard_CV_Kelly_Battistoni_EN.pdf` downloads (check browser download bar).
 f. Click "ES" picker, then click "Download". Confirm `Harvard_CV_Kelly_Battistoni_ES.pdf` downloads.
 g. Switch the site language to ES (PillNav switcher). Leave the CV picker on EN. Click "Download". Confirm the EN PDF still downloads — the CV picker is INDEPENDENT of site language (research Pitfall 3 sanity check).
 h. Read the invite copy in both EN and ES. Edit `src/locales/{en,es}/contact.json` to refine.

Additional cross-section checks:
 i. Open browser DevTools console. Confirm no React warnings, no `parseMissingKeyHandler` output (no `about:title` or similar in console), no GSAP warnings.
 j. Open mobile DevTools emulation (≤ 430px). Confirm sections stack vertically, ParallaxCards skip parallax (cards render at rest position, content visible). MobileNav (hamburger) replaces PillNav.
 k. Use keyboard Tab to navigate from Hero CTA through to Contact CV download. Confirm a visible focus indicator on every interactive element.
 l. Reload the page with the site set to ES. Confirm ES persists (Phase 2's localStorage `kbv-lang`).

If ANYTHING fails, describe the issue. Common fixes:
- Stack icon renders as black blob → Pitfall 2: verify the three inline SVG components use `fill={color}` with `color = 'currentColor'`.
- CV picker doesn't independence from site language → Pitfall 3: verify `cvHref` reads from `cvLang` useState, NOT from `i18n.language`.
- Project card stagger fires together not sequentially → Pitfall 5: verify direct children of RevealSection (no wrapper divs around card grid).
- LinkedIn URL is literally "LINKEDIN_URL" → EXPECTED behavior — flag for Phase 7 swap.
  </how-to-verify>
  <resume-signal>Type "approved" once all four ROADMAP Phase 6 criteria pass and copy iteration is complete. Describe any issue blocking phase closure and the verification re-runs after fixes.</resume-signal>
</task>

</tasks>

<verification>
- `src/App.tsx` imports About, Projects, Stack, Contact, AnimationErrorBoundary
- `src/App.tsx` no longer contains the 200vh placeholder div
- `src/App.tsx` wraps each new section in its own AnimationErrorBoundary
- `npm run build` succeeds
- `npm run lint` is clean
- `npm run dev` boots without console errors
- User confirms all 4 ROADMAP Phase 6 success criteria pass in browser
- User confirms NDA-safe copy review complete (no org names, no proprietary internals)
- User confirms `LINKEDIN_URL` placeholder visible (flagged for Phase 7 — does not block Phase 6 closure per locked decision)
</verification>

<success_criteria>
1. App.tsx renders the full page in order: Noise → Hero → Nav → About → Projects → Stack → Contact.
2. Each new section is in its own AnimationErrorBoundary.
3. All 4 ROADMAP Phase 6 success criteria pass user browser verification.
4. NDA-safe copy review for Projects is complete in both EN and ES.
5. CV download serves the chosen-language PDF independent of site language.
6. `LINKEDIN_URL` placeholder is acknowledged and flagged for Phase 7 (does NOT block phase closure).
7. No console errors, no untranslated key leakage, no broken GSAP triggers.
8. Phase 6 closes and STATE.md is updated to mark Phase 6 Complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-sections/06-06-SUMMARY.md`.
</output>

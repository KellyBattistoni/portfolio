---
phase: 07-polish-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/plasma/HeroBackdrop.tsx
  - src/components/hero/Hero.tsx
  - src/styles/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "HeroBackdrop loads asynchronously — the main JS bundle no longer includes OGL/Plasma"
    - "Suspense boundary wraps HeroBackdrop inside the existing AnimationErrorBoundary"
    - "PlasmaFallback renders while HeroBackdrop chunk is loading"
    - "All interactive elements show a 2px solid #FF4500 focus ring when focused via keyboard"
    - "Focus ring appears on Tab navigation but not on mouse click"
  artifacts:
    - path: "src/components/plasma/HeroBackdrop.tsx"
      provides: "Default export enabling React.lazy() import"
      contains: "export default HeroBackdrop"
    - path: "src/components/hero/Hero.tsx"
      provides: "Lazy-loaded HeroBackdrop with Suspense boundary"
      contains: "React.lazy"
    - path: "src/styles/index.css"
      provides: "Global :focus-visible rule"
      contains: ":focus-visible"
  key_links:
    - from: "src/components/hero/Hero.tsx"
      to: "src/components/plasma/HeroBackdrop.tsx"
      via: "React.lazy(() => import('@/components/plasma/HeroBackdrop'))"
      pattern: "lazy\\("
    - from: "src/styles/index.css"
      to: "all interactive elements"
      via: ":focus-visible selector"
      pattern: "focus-visible"
---

<objective>
Split the HeroBackdrop/Plasma/OGL chunk out of the main bundle via React.lazy() + Suspense, and add the global :focus-visible focus ring CSS. These two changes are independent and can be implemented in any order.

Purpose: Reduce main bundle from ~510KB to ~440KB (drops Vite's >500KB warning and improves Lighthouse Performance score). Add visible keyboard focus indicator to all ~46 interactive elements in one CSS rule (WCAG 2.4.7 Focus Visible + D-12).

Output: HeroBackdrop with default export, Hero.tsx with lazy import + Suspense boundary, index.css with :focus-visible rule.
</objective>

<execution_context>
@C:\Users\KLBVT\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\KLBVT\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-performance/07-CONTEXT.md
@.planning/phases/07-polish-performance/07-RESEARCH.md
@.planning/phases/07-polish-performance/07-PATTERNS.md

<interfaces>
<!-- Current HeroBackdrop.tsx — named export only (line 13, line 76 is closing brace) -->
<!-- React.lazy() requires a default export — add one line at the end of the file -->

src/components/plasma/HeroBackdrop.tsx current export (line 13):
```typescript
export function HeroBackdrop({ heroRef }: HeroBackdropProps) {
```

src/components/hero/Hero.tsx current import (line 37):
```typescript
import { HeroBackdrop } from '@/components/plasma/HeroBackdrop'
```

src/components/hero/Hero.tsx current JSX usage (lines 159-161):
```tsx
<AnimationErrorBoundary>
  <HeroBackdrop heroRef={sectionRef} />
</AnimationErrorBoundary>
```

src/styles/index.css current end state (lines 44-49):
```css
.mobile-nav-panel {
  transform: translateX(100%);
}
```
<!-- File ends after this rule — no :focus-visible rule present -->

src/components/plasma/PlasmaFallback.tsx — confirmed pure CSS, no WebGL imports (safe as Suspense fallback)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add default export to HeroBackdrop + wire React.lazy in Hero.tsx</name>
  <files>src/components/plasma/HeroBackdrop.tsx, src/components/hero/Hero.tsx</files>
  <read_first>
    - src/components/plasma/HeroBackdrop.tsx — confirm the component ends with the closing brace on line 76; add default export on line 77
    - src/components/hero/Hero.tsx — confirm the eager import line (line 37) and the AnimationErrorBoundary JSX block (lines 159-161) before modifying
    - src/components/plasma/PlasmaFallback.tsx — confirm no WebGL imports (must be safe as Suspense fallback)
  </read_first>
  <action>
    Step 1 — Edit src/components/plasma/HeroBackdrop.tsx:
    - Append on the line after the closing brace of the HeroBackdrop function: export default HeroBackdrop
    - The named export (export function HeroBackdrop) MUST remain — it is still imported by type references and HeroBackdropProps consumers. Only ADD the default export line; do not change the named export.

    Step 2 — Edit src/components/hero/Hero.tsx:
    - Remove the line: import { HeroBackdrop } from '@/components/plasma/HeroBackdrop'
    - Add these two lines in its place (after the AnimationErrorBoundary import):
        import { lazy, Suspense } from 'react'
        import { PlasmaFallback } from '@/components/plasma/PlasmaFallback'
    - Add this const declaration after the imports and before the HeroProps interface:
        const HeroBackdrop = lazy(() => import('@/components/plasma/HeroBackdrop'))
    - Find the AnimationErrorBoundary JSX block and wrap the HeroBackdrop element with a Suspense boundary:
        Current: <AnimationErrorBoundary><HeroBackdrop heroRef={sectionRef} /></AnimationErrorBoundary>
        After:   <AnimationErrorBoundary><Suspense fallback={<PlasmaFallback />}><HeroBackdrop heroRef={sectionRef} /></Suspense></AnimationErrorBoundary>
    - The Suspense boundary goes INSIDE the AnimationErrorBoundary (not outside) so that async chunk load failures are caught by the error boundary rather than surfacing an unhandled error. This is the pattern documented in RESEARCH.md Pitfall 2.
    - The lazy() import path '@/components/plasma/HeroBackdrop' uses the default export (no curly braces) — React.lazy always reads .default from the dynamic import result.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/plasma/HeroBackdrop.tsx contains the line "export default HeroBackdrop" (exact text)
    - src/components/hero/Hero.tsx no longer contains the line "import { HeroBackdrop } from '@/components/plasma/HeroBackdrop'"
    - src/components/hero/Hero.tsx contains "const HeroBackdrop = lazy("
    - src/components/hero/Hero.tsx contains "Suspense fallback={<PlasmaFallback />}"
    - npm run build exits with code 0
    - Build output shows TWO JS chunks (main chunk ~440-450KB AND a separate async chunk for HeroBackdrop ~66KB)
    - The Vite ">500KB chunk" warning is absent from the build output
  </acceptance_criteria>
  <done>HeroBackdrop splits into its own async chunk; Suspense + fallback wired; build succeeds without bundle size warning</done>
</task>

<task type="auto">
  <name>Task 2: Add global :focus-visible rule to index.css</name>
  <files>src/styles/index.css</files>
  <read_first>
    - src/styles/index.css — read the full file; confirm it ends after the .mobile-nav-panel rule and has no existing :focus-visible rule
  </read_first>
  <action>
    Edit src/styles/index.css — append the following CSS rule after the .mobile-nav-panel block (the last rule currently in the file):

    Add a blank line separator, then a comment "D-12: Brand focus ring — visible on #050505 background", then the rule:
      :focus-visible {
        outline: 2px solid #FF4500;
        outline-offset: 2px;
      }

    Use :focus-visible (NOT :focus) — :focus-visible only activates on keyboard navigation, not on mouse click. This matches D-12 spec and WCAG 2.4.7. All three target browsers (Chrome, Safari 16.4+, Firefox) support :focus-visible.

    This single rule covers all 46 interactive elements in the application: Hero CTA anchor, PillNav anchors (4), LanguageSwitcher buttons (2 each in desktop and mobile), MobileNav hamburger button and panel anchors (4), Stack tile links (~29), Contact email link, LinkedIn CTA, CV language radio buttons (2), and CV download link.

    No per-component CSS changes are needed — this global selector handles all elements automatically.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/index.css contains the text ":focus-visible" (exact selector)
    - src/styles/index.css contains "outline: 2px solid #FF4500"
    - src/styles/index.css contains "outline-offset: 2px"
    - The :focus-visible rule appears AFTER the .mobile-nav-panel rule (not before)
    - src/styles/index.css does NOT contain a bare ":focus {" rule (would override on mouse click too)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Global focus ring CSS added; all 46 interactive elements covered by one rule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → async chunk | React.lazy loads HeroBackdrop chunk from the same origin (GitHub Pages CDN) — no cross-origin chunk loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | HeroBackdrop async chunk load | mitigate | Suspense inside AnimationErrorBoundary — network failure shows PlasmaFallback via error boundary, not a blank screen |
| T-07-04 | Spoofing | :focus-visible indicator | accept | Outlines are cosmetic UI — no auth or security boundary crossed |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After task completion, verify the full plan:

1. npm run build exits with code 0 with no bundle size warnings
2. Build output contains two JS chunks (main and async HeroBackdrop)
3. src/components/plasma/HeroBackdrop.tsx contains "export default HeroBackdrop"
4. src/components/hero/Hero.tsx contains "const HeroBackdrop = lazy("
5. src/styles/index.css contains ":focus-visible { outline: 2px solid #FF4500; outline-offset: 2px; }"
</verification>

<success_criteria>
- Main JS bundle reduced from ~510KB to ~440-450KB (Vite >500KB warning gone)
- HeroBackdrop loads asynchronously — Plasma/OGL split into separate chunk
- All interactive elements show #FF4500 focus ring on keyboard navigation (WCAG 2.4.7)
- Mouse click does NOT trigger the focus ring (correct :focus-visible behavior)
- TypeScript and build both clean
</success_criteria>

<output>
Create .planning/phases/07-polish-performance/07-02-SUMMARY.md when done.
Record actual bundle sizes from the build output in the summary.
</output>

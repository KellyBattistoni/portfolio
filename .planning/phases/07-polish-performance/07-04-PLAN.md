---
phase: 07-polish-performance
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - package.json
  - playwright.config.ts
autonomous: false
requirements: []
roadmap_success_criteria:
  - SC-1   # Lighthouse ≥ 90 + LCP < 2.5s (final audit)
  - SC-2   # Keyboard focus ring + contrast confirmed via browser checkpoint
  - SC-3   # prefers-reduced-motion verified via browser checkpoint
  - SC-4   # Mobile 375/430px, Safari WebKit smoke test, og-image.png, hreflang confirmed
user_setup:
  - service: playwright-webkit
    why: "Safari cross-browser testing (D-14)"
    env_vars: []
    dashboard_config:
      - task: "Install Playwright WebKit browser binary"
        location: "Terminal — run: npx playwright install webkit (after npm install)"

must_haves:
  truths:
    - "Lighthouse mobile scores all ≥ 90 on Performance, Accessibility, Best Practices, and SEO"
    - "LCP is under 2.5 seconds on mobile Lighthouse audit"
    - "prefers-reduced-motion: reduce disables all animations across all 9 components"
    - "Site renders correctly at 375px and 430px viewport widths"
    - "Playwright WebKit smoke test passes — scroll, nav, and hero render without errors in Safari engine"
    - "All interactive elements show #FF4500 focus ring when keyboard-navigated (Tab key)"
    - "public/og-image.png exists at 1200×630px"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright config with WebKit project"
      contains: "webkit"
    - path: "public/og-image.png"
      provides: "OG image for social previews — MUST exist from Plan 07-01 checkpoint"
  key_links:
    - from: "playwright.config.ts"
      to: "WebKit browser binary"
      via: "npx playwright install webkit"
      pattern: "webkit"
---

<objective>
Install Playwright + WebKit browser binary, run the Safari smoke test, perform mobile QA at 375/430px, verify prefers-reduced-motion behavior, run the final Lighthouse audit, and confirm all four ROADMAP Phase 7 success criteria pass via human browser checkpoint.

Purpose: This plan is the quality gate — it confirms that every improvement made in Plans 07-01 through 07-03 meets the ROADMAP launch criteria before Phase 7 is closed.

Output: playwright.config.ts, @playwright/test in package.json devDependencies, passing WebKit smoke test, confirmed Lighthouse ≥ 90 scores, human approval on all four success criteria.

Pre-condition: public/og-image.png must exist before this plan runs. It is created at the Plan 07-01 checkpoint when the user screenshots public/og-card-preview.html. Task 3 of this plan confirms its existence as part of SC-4 verification.
</objective>

<execution_context>
@C:\Users\KLBVT\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\KLBVT\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-performance/07-CONTEXT.md
@.planning/phases/07-polish-performance/07-RESEARCH.md

<interfaces>
<!-- Phase 7 ROADMAP success criteria (all four must pass) -->

SC-1: Lighthouse mobile audit ≥ 90 on Performance, Accessibility, Best Practices, SEO; LCP < 2.5s
SC-2: Full keyboard tab navigation with visible focus indicator; body text 4.5:1 contrast on #050505
SC-3: prefers-reduced-motion: reduce disables Plasma, scroll-reveal, parallax, and PillNav entrance animations
SC-4: Correct rendering at 375-430px, Chrome + Safari 16.4+ + Firefox; OG 1200×630 image; per-language title/hreflang in previews

<!-- Playwright WebKit environment note from RESEARCH.md -->
Only Chromium (chromium-1208) is currently installed. WebKit binary does not exist.
Must run: npx playwright install webkit (after npm install adds @playwright/test)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @playwright/test + WebKit binary + create playwright.config.ts</name>
  <files>package.json, playwright.config.ts</files>
  <read_first>
    - package.json — confirm @playwright/test is not yet in devDependencies before adding
  </read_first>
  <action>
    Step 1 — Install @playwright/test as a devDependency:
    Run: npm install --save-dev @playwright/test@1.61.0

    Step 2 — Install the WebKit browser binary:
    Run: npx playwright install webkit
    This downloads the WebKit engine binary required for D-14 Safari testing. Only Chromium is currently installed; WebKit must be explicitly installed. (RESEARCH.md Environment Availability, Pitfall 7)

    Step 3 — Create playwright.config.ts in the project root with a minimal configuration:
    - Import defineConfig and devices from '@playwright/test'
    - Set testDir to './e2e'
    - Set baseURL to 'http://localhost:4173' (vite preview port)
    - Set one project: name 'webkit', use devices['Desktop Safari'] for WebKit engine
    - Set fullyParallel to false (single project, no parallelism needed)
    - Set retries to 0
    - Set reporter to 'list'

    Step 4 — Create the e2e/ directory and a smoke test file e2e/webkit-smoke.spec.ts:
    The smoke test must:
    - Navigate to '/' (baseURL)
    - Assert the page title contains "Kelly Battistoni"
    - Assert the hero section is visible: page.locator('section[aria-label="Hero"]') is visible
    - Scroll to the bottom of the page: page.evaluate(() => window.scrollTo(0, document.body.scrollHeight))
    - Assert no console errors of type 'error' were fired during the scroll
    - Assert the Stack section is visible after scroll: page.locator('#stack') is visible

    Do NOT write complex assertions — this is a smoke test confirming the page loads and scrolls in WebKit without crashing.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vite preview --port 4173 &amp; sleep 3 &amp;&amp; npx playwright test --project=webkit 2>&amp;1; kill %1</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "@playwright/test" at version 1.61.x
    - playwright.config.ts exists in the project root and contains "webkit"
    - e2e/webkit-smoke.spec.ts exists and contains "Kelly Battistoni"
    - npx playwright install webkit completes without error
    - npx playwright test --project=webkit runs and all tests pass (green)
    - No "Executable doesn't exist" error for WebKit (RESEARCH.md Pitfall 7 resolved)
  </acceptance_criteria>
  <done>@playwright/test installed, WebKit binary installed, smoke test passes in WebKit engine</done>
</task>

<task type="auto">
  <name>Task 2: Final Lighthouse Audit</name>
  <files>No files modified — audit only</files>
  <read_first>
    - No files to read — this task runs the final production Lighthouse audit
  </read_first>
  <action>
    Run the final Lighthouse mobile audit against the production build to confirm all four scores are ≥ 90 and LCP is under 2.5s:

    1. Run: npm run build
    2. In a separate terminal, run: npx vite preview --port 4173
    3. Open Chrome DevTools → Lighthouse panel → Mobile device preset → run audit against http://localhost:4173
    4. Record all four category scores: Performance, Accessibility, Best Practices, SEO
    5. Record LCP from Performance diagnostics
    6. Compare against baseline from Plan 07-01 SUMMARY

    Expected improvements vs baseline (from RESEARCH.md):
    - Performance: bundle split (~66KB removed from main chunk) improves LCP timing
    - Accessibility: focus rings + contrast fixes raise the Accessibility score
    - SEO: meta tags + hreflang improve SEO score
    - Best Practices: no specific changes, should remain unchanged

    If any score is below 90:
    - Performance below 90: check if React.lazy split was successful (verify HeroBackdrop is in a separate chunk)
    - Accessibility below 90: run DevTools Accessibility panel to identify remaining issues; common causes: missing alt attributes, missing aria-labels on icon-only buttons
    - SEO below 90: verify meta description is present in the production build (not just the SPA post-JS state)

    Stop the vite preview server after recording scores.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - Build output shows two JS chunks (main + async HeroBackdrop)
    - Lighthouse Performance score is documented (target ≥ 90)
    - Lighthouse Accessibility score is documented (target ≥ 90)
    - Lighthouse Best Practices score is documented (target ≥ 90)
    - Lighthouse SEO score is documented (target ≥ 90)
    - LCP value is documented (target < 2.5s)
  </acceptance_criteria>
  <done>Final Lighthouse scores recorded; confirmed against ≥ 90 targets</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <!-- Estimated time: 15–30 min for full verification; Lighthouse retests add 2–3 min each -->
  <what-built>
    Plan 07-01: useMetaTags hook, OG meta tags, hreflang, OG card preview HTML, public/og-image.png (created at Plan 07-01 checkpoint)
    Plan 07-02: React.lazy HeroBackdrop bundle split, global :focus-visible focus rings
    Plan 07-03: WCAG AA contrast fixes across Stack, About, and Contact
    Plan 07-04: Playwright WebKit installed, smoke test passing, final Lighthouse audit run
  </what-built>
  <how-to-verify>
    Run: npm run dev (for interactive browser testing below)

    SC-1 — Lighthouse ≥ 90:
    - Run the Lighthouse audit from Task 2 above (npm run build + npx vite preview)
    - Confirm: Performance ≥ 90, Accessibility ≥ 90, Best Practices ≥ 90, SEO ≥ 90
    - Confirm: LCP < 2.5s

    SC-2 — Keyboard navigation + contrast:
    - Open http://localhost:5173, press Tab repeatedly
    - Confirm: each interactive element (Hero CTA, PillNav links, LanguageSwitcher, Stack tiles, Contact links, CV buttons) shows a visible #FF4500 orange focus ring
    - Confirm: no element is skipped in the tab order
    - Visual spot-check: Stack tile names, arc descriptions, stats labels, contact method labels — they should be clearly readable (not as faint as before)

    SC-3 — Reduced motion:
    - In Chrome DevTools → More Tools → Rendering → "Emulate CSS prefers-reduced-motion: reduce"
    - Scroll the page — confirm: no reveal animations, no PillNav entrance animation, PlasmaFallback shown instead of Plasma WebGL (static gradient, no pulse), no parallax movement on project cards

    SC-4 — Mobile and cross-browser:
    - In DevTools, toggle to Responsive mode, set width to 375px — scroll through all sections (Hero, About, Projects, Stack, Contact) and confirm nothing is clipped, overlapping, or broken
    - Set width to 430px and repeat
    - Confirm public/og-image.png exists in the project root's public/ directory (must have been created at Plan 07-01 checkpoint — if not present, open public/og-card-preview.html in Chrome, screenshot at 1200×630, and save as public/og-image.png before proceeding)
    - Verify language title/hreflang: open DevTools → Elements → find the head; confirm hreflang links are present and document.title shows the correct language after toggling EN/ES

    Firefox check (optional but recommended):
    - Open http://localhost:5173 in Firefox
    - Confirm hero renders, PillNav appears on scroll, animations work

    Check all four ROADMAP SC boxes above and confirm pass/fail for each.
  </how-to-verify>
  <resume-signal>Report results for each SC:
  SC-1 (Lighthouse): [score results or "PASS ≥ 90"]
  SC-2 (Keyboard/contrast): [PASS or issues found]
  SC-3 (Reduced motion): [PASS or issues found]
  SC-4 (Mobile/cross-browser/OG): [PASS or issues found]

  If all pass, type "approved". If any fail, describe the issue and Claude will diagnose and fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → localhost:4173 | Test runs against local production build — no external network requests in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service | Playwright WebKit test runner | accept | Local devDependency only; not in production bundle |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | @playwright/test 1.61.0 verified in Package Legitimacy Audit (RESEARCH.md) — disposition [OK] |
</threat_model>

<verification>
After task completion, verify the full phase:

1. npm run build exits with code 0 with no bundle size warnings
2. npx playwright test --project=webkit exits with 0 failures
3. Lighthouse mobile scores: Performance ≥ 90, Accessibility ≥ 90, Best Practices ≥ 90, SEO ≥ 90
4. LCP < 2.5s
5. public/og-image.png exists (confirmed at Plan 07-01 checkpoint, re-checked at Task 3 checkpoint)
6. Human checkpoint confirms all 4 ROADMAP Phase 7 success criteria pass
</verification>

<success_criteria>
- All four Lighthouse mobile scores ≥ 90 (SC-1)
- LCP < 2.5s on mobile (SC-1)
- Tab navigation reaches every interactive element with #FF4500 visible focus ring (SC-2)
- All WCAG AA contrast failures resolved — confirmed in browser (SC-2)
- prefers-reduced-motion disables all animations site-wide (SC-3)
- Site renders correctly at 375px and 430px viewports (SC-4)
- Playwright WebKit smoke test passes (SC-4 Safari coverage)
- public/og-image.png exists at 1200×630 (SC-4)
- Per-language title (EN/ES) and hreflang tags confirmed in browser (SC-4)
- User has typed "approved" at the final checkpoint
</success_criteria>

<output>
Create .planning/phases/07-polish-performance/07-04-SUMMARY.md when done.
Record final Lighthouse scores (all four categories + LCP) in the summary.
Mark Phase 7 complete in STATE.md and ROADMAP.md.
</output>

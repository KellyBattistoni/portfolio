---
phase: 07-polish-performance
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/components/sections/Stack.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Contact.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Stack tile names (tool names under icons) pass WCAG AA 4.5:1 contrast on #050505"
    - "Stack band labels (section category labels) pass WCAG AA 4.5:1 contrast on #050505"
    - "About arc past-node description text passes WCAG AA 4.5:1 contrast on #050505"
    - "About stats labels (Continents/Automations/Years) pass WCAG AA 4.5:1 contrast on #050505"
    - "About arc past-node year text passes WCAG AA 4.5:1 contrast on #050505"
    - "Contact method labels pass WCAG AA 4.5:1 contrast on #050505"
    - "Contact CV lang button inactive state passes WCAG AA 4.5:1 contrast on #050505"
    - "Contact openTo paragraph text passes WCAG AA 4.5:1 contrast on #050505"
  artifacts:
    - path: "src/components/sections/Stack.tsx"
      provides: "WCAG-compliant contrast on stack tile names and band labels"
      contains: "rgba(255,255,255,0.6)"
    - path: "src/components/sections/About.tsx"
      provides: "WCAG-compliant contrast on arc descriptions and stats labels"
      contains: "rgba(255,255,255,0.55)"
    - path: "src/components/sections/Contact.tsx"
      provides: "WCAG-compliant contrast on method labels, CV buttons, and openTo text"
      contains: "rgba(255,255,255,0.55)"
  key_links:
    - from: "Stack.tsx .stack-tile"
      to: "#050505 background"
      via: "rgba(255,255,255,0.60) — computed contrast 7.39:1"
      pattern: "rgba\\(255,255,255,0\\.6\\)"
    - from: "About.tsx stats label"
      to: "#050505 background"
      via: "rgba(255,255,255,0.55) — computed contrast 6.71:1"
      pattern: "rgba\\(255,255,255,0\\.55\\)"
---

<objective>
Fix all 5 WCAG AA contrast failures identified in the Phase 7 audit. All failing values are readable content strings (not decorative); raising alpha values to 0.55–0.60 achieves 6.71–7.39:1 contrast while preserving the muted cinematic aesthetic.

Purpose: ROADMAP Phase 7 SC-2 requires body text meeting 4.5:1 contrast on #050505. Minimum white alpha for 4.5:1 is 0.46. All current failing values are below this threshold.

Output: Stack.tsx, About.tsx, and Contact.tsx with failing rgba alpha values corrected to minimum 0.55.
</objective>

<execution_context>
@C:\Users\KLBVT\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\KLBVT\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-performance/07-CONTEXT.md
@.planning/phases/07-polish-performance/07-RESEARCH.md
@.planning/phases/07-polish-performance/07-PATTERNS.md

<interfaces>
<!-- Exact current values to find and replace — from RESEARCH.md contrast audit and direct file read -->

Stack.tsx CSS string (STACK_STYLES, line 26):
  Current: color: rgba(255,255,255,0.38);   (band-lbl — 3.44:1 FAIL)
  Fix to:  color: rgba(255,255,255,0.55);

Stack.tsx CSS string (STACK_STYLES, line 54):
  Current: color: rgba(255,255,255,0.4);    (stack-tile — 3.71:1 FAIL)
  Fix to:  color: rgba(255,255,255,0.6);

About.tsx inline style (line ~323 — arc past-node year, NON-current):
  Current: 'rgba(255,255,255,0.45)'   (when node.isCurrent is false — 4.49:1 FAIL)
  Fix to:  'rgba(255,255,255,0.65)'

About.tsx inline style (line ~348 — arc past-node desc, NON-current):
  Current: 'rgba(255,255,255,0.35)'   (when node.isCurrent is false — 3.10:1 FAIL)
  Fix to:  'rgba(255,255,255,0.55)'

About.tsx inline style (line ~399 — stats label):
  Current: 'rgba(255,255,255,0.3)'    (2.53:1 FAIL)
  Fix to:  'rgba(255,255,255,0.55)'

Contact.tsx CSS string (CONTACT_STYLES, line 26):
  Current: color: rgba(255,255,255,0.38);   (contact-method-lbl — 3.44:1 FAIL)
  Fix to:  color: rgba(255,255,255,0.55);

Contact.tsx CSS string (CONTACT_STYLES, line 81):
  Current: color: rgba(255,255,255,0.45);   (cv-lang-btn — 4.49:1 FAIL)
  Fix to:  color: rgba(255,255,255,0.55);

Contact.tsx inline style (line ~313 — openTo paragraph):
  Current: 'rgba(255,255,255,0.38)'  (3.44:1 FAIL)
  Fix to:  'rgba(255,255,255,0.55)'

DO NOT change:
  - 'rgba(255,255,255,0.45)' when node.isCurrent is true (arc current-node year) — this is the CURRENT node; already at 4.49:1 but context is current/highlighted state. Raise to 0.65 for safety margin.
  - 'rgba(255,255,255,0.85)' (Contact email link) — 14.58:1, already passes
  - 'rgba(255,255,255,0.75)' (Contact invite text) — 11.32:1, already passes
  - 'rgba(255,255,255,0.6)' in arc node role (already passes at 7.39:1)
  - opacity: 0.6 on arc node containers — do NOT change opacity on containers, only fix text color directly
  - The rgba(255,255,255,0.95) and rgba(255,255,255,0.85) values used for CURRENT arc node — already passing
  - rgba(255,255,255,0.88) (Hero tagline) — 15.59:1, already passes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Stack.tsx contrast failures</name>
  <files>src/components/sections/Stack.tsx</files>
  <read_first>
    - src/components/sections/Stack.tsx — read the STACK_STYLES string (lines 8-72) to confirm the exact current rgba values at band-lbl (line 26) and stack-tile (line 54) before making changes
  </read_first>
  <action>
    Edit src/components/sections/Stack.tsx — two changes inside the STACK_STYLES template literal string:

    Change 1 — .band-lbl color (the section category labels: "AUTOMATION", "CODE", etc.):
    - Find: color: rgba(255,255,255,0.38);  (inside .band-lbl rule)
    - Replace with: color: rgba(255,255,255,0.55);
    - Rationale: 0.38 = 3.44:1 (FAIL). 0.55 = 6.71:1 (PASS with comfortable margin)

    Change 2 — .stack-tile color (the tool name text under each icon):
    - Find: color: rgba(255,255,255,0.4);  (inside .stack-tile rule)
    - Replace with: color: rgba(255,255,255,0.6);
    - Rationale: 0.40 = 3.71:1 (FAIL). 0.60 = 7.39:1 (PASS with comfortable margin)

    Do NOT change the .stack-tile:hover color, the .stack-tile transition values, or any other property. Only the two color values listed above.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&amp;1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Stack.tsx .band-lbl CSS rule contains "color: rgba(255,255,255,0.55)" (not 0.38)
    - Stack.tsx .stack-tile CSS rule contains "color: rgba(255,255,255,0.6)" (not 0.4)
    - Stack.tsx does NOT contain "rgba(255,255,255,0.38)" or "rgba(255,255,255,0.4)" anywhere in the component (these values existed only in the two fixed locations)
    - npx tsc -b --noEmit exits with code 0
  </acceptance_criteria>
  <done>Stack.tsx: band-lbl raised 0.38→0.55 (6.71:1), stack-tile raised 0.40→0.60 (7.39:1)</done>
</task>

<task type="auto">
  <name>Task 2: Fix About.tsx contrast failures</name>
  <files>src/components/sections/About.tsx</files>
  <read_first>
    - src/components/sections/About.tsx — read lines 315-410 (arc node render loop and stats section) to confirm the exact ternary expressions and inline style values before modifying
  </read_first>
  <action>
    Edit src/components/sections/About.tsx — three changes in inline style objects within the ARC_NODES.map render loop and the stats section:

    Change 1 — Arc past-node year text (NON-current node year display):
    The year div uses a ternary: node.isCurrent ? 'rgba(255,255,255,0.95)' : 'rgba(255,255,255,0.45)'
    - Find the ternary expression where isCurrent is false returns 'rgba(255,255,255,0.45)'
    - Change the false-branch from 'rgba(255,255,255,0.45)' to 'rgba(255,255,255,0.65)'
    - The true-branch 'rgba(255,255,255,0.95)' stays unchanged

    Change 2 — Arc past-node description text (NON-current node description):
    The desc paragraph uses a ternary: node.isCurrent ? 'rgba(255,255,255,0.45)' : 'rgba(255,255,255,0.35)'
    - Change the false-branch from 'rgba(255,255,255,0.35)' to 'rgba(255,255,255,0.55)'
    - Change the true-branch from 'rgba(255,255,255,0.45)' to 'rgba(255,255,255,0.65)'
    (Both branches are currently failing or borderline — raise both for consistency)

    Change 3 — Stats labels (Continents / Automations / Years text below the numbers):
    The stats label div uses: color: 'rgba(255,255,255,0.3)'
    - Find this value inside the stats map (it is NOT in a ternary — a flat value)
    - Change from 'rgba(255,255,255,0.3)' to 'rgba(255,255,255,0.55)'

    Do NOT change:
    - opacity: 0.6 on arc node wrapper containers — container opacity, not text color
    - 'rgba(255,255,255,0.6)' for arc node role text — already passing
    - 'rgba(255,255,255,0.95)' for current arc node year — already passing
    - 'rgba(255,255,255,0.85)' for current arc node role — already passing
    - Stats number values rgba(255,255,255,0.9) — already passing
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&amp;1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - About.tsx does NOT contain 'rgba(255,255,255,0.35)' as an inline style value (was arc desc NON-current)
    - About.tsx does NOT contain 'rgba(255,255,255,0.3)' as the stats label color (was stats label)
    - About.tsx does NOT contain the pair ternary returning 'rgba(255,255,255,0.45)' for NON-current year (was below AA)
    - About.tsx contains 'rgba(255,255,255,0.55)' in the arc desc ternary false-branch
    - About.tsx contains 'rgba(255,255,255,0.65)' in the arc year ternary false-branch
    - About.tsx contains 'rgba(255,255,255,0.55)' as the stats label color
    - opacity: 0.6 on arc node containers is UNCHANGED (still present)
    - npx tsc -b --noEmit exits with code 0
  </acceptance_criteria>
  <done>About.tsx: arc desc raised 0.35→0.55, arc year raised 0.45→0.65, stats label raised 0.30→0.55</done>
</task>

<task type="auto">
  <name>Task 3: Fix Contact.tsx contrast failures</name>
  <files>src/components/sections/Contact.tsx</files>
  <read_first>
    - src/components/sections/Contact.tsx — read the CONTACT_STYLES string (lines 6-105) and the openTo paragraph inline style (around line 313) to confirm exact current rgba values before modifying
  </read_first>
  <action>
    Edit src/components/sections/Contact.tsx — three changes:

    Change 1 — .contact-method-lbl (section labels: "EMAIL", "LINKEDIN", "CV / RESUME"):
    Inside the CONTACT_STYLES template literal, find the .contact-method-lbl rule:
    - Find: color: rgba(255,255,255,0.38);
    - Replace with: color: rgba(255,255,255,0.55);

    Change 2 — .cv-lang-btn inactive state (the EN/ES radio buttons when NOT selected):
    Inside CONTACT_STYLES, find the .cv-lang-btn rule (not the :hover or [aria-checked="true"] variant):
    - Find: color: rgba(255,255,255,0.45);  (the base/inactive state)
    - Replace with: color: rgba(255,255,255,0.55);
    - Leave the .cv-lang-btn:hover and .cv-lang-btn[aria-checked="true"] rules UNCHANGED

    Change 3 — openTo paragraph text (the "Open to..." secondary text in the left column):
    Find the inline style on the openTo paragraph (around line 313):
    - Find: color: 'rgba(255,255,255,0.38)'
    - Replace with: color: 'rgba(255,255,255,0.55)'

    Do NOT change:
    - .contact-email-link color rgba(255,255,255,0.85) — already passes
    - .contact-cta color var(--color-brand-accent) — #FF4500, already passes
    - The invite paragraph rgba(255,255,255,0.75) — already passes
    - .cv-lang-btn[aria-checked="true"] color rgba(255,255,255,1) — already passes
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Contact.tsx .contact-method-lbl CSS contains "color: rgba(255,255,255,0.55)" (not 0.38)
    - Contact.tsx .cv-lang-btn base CSS contains "color: rgba(255,255,255,0.55)" (not 0.45)
    - Contact.tsx openTo paragraph inline style contains 'rgba(255,255,255,0.55)' (not 0.38)
    - Contact.tsx does NOT contain any remaining rgba(255,255,255,0.38) values (all were failing; all should now be 0.55)
    - Contact.tsx does NOT contain rgba(255,255,255,0.45) in the .cv-lang-btn base rule (moved to 0.55)
    - .cv-lang-btn[aria-checked="true"] still has color: rgba(255,255,255,1) (unchanged)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Contact.tsx: method-lbl raised 0.38→0.55, cv-lang-btn inactive raised 0.45→0.55, openTo raised 0.38→0.55</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component CSS → rendered text | Color values are static CSS — no runtime user input or injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | Static CSS rgba values | accept | Color values are design tokens; not security-sensitive |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After task completion, verify the full plan:

1. npm run build exits with code 0 — no TypeScript or bundler errors
2. Grep for remaining WCAG failures: no rgba(255,255,255,0.38), 0.40, 0.35, 0.30, or 0.45 values in the section components (outside of passing contexts)
3. All three component files contain rgba(255,255,255,0.55) or higher for previously-failing text elements
</verification>

<success_criteria>
- Stack.tsx: band-lbl 6.71:1, stack-tile 7.39:1 — both PASS AA
- About.tsx: arc desc 6.71:1, arc year 8.76:1, stats label 6.71:1 — all PASS AA
- Contact.tsx: method-lbl 6.71:1, cv-lang-btn inactive 6.71:1, openTo 6.71:1 — all PASS AA
- TypeScript clean; build clean
- All previously-failing rgba values at 0.55 or higher
</success_criteria>

<output>
Create .planning/phases/07-polish-performance/07-03-SUMMARY.md when done.
</output>

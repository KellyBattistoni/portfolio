---
phase: 07-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useMetaTags.ts
  - src/locales/en/common.json
  - src/locales/es/common.json
  - src/App.tsx
  - index.html
autonomous: false
requirements: []
roadmap_success_criteria:
  - SC-1   # Lighthouse baseline documented before fixes
  - SC-4   # OG tags, hreflang, og-image.png, per-language title

must_haves:
  truths:
    - "document.title updates to the correct language-specific title when the EN/ES switcher is toggled"
    - "meta[name='description'] updates to the correct language-specific description on language change"
    - "index.html contains og:title, og:description, og:image, og:url, og:type, og:image:width, og:image:height, and twitter:card meta tags in static HTML"
    - "index.html contains three hreflang link elements (en, es, x-default) pointing to https://kellybattistoni.github.io/"
    - "Baseline Lighthouse scores are recorded before any performance fixes"
  artifacts:
    - path: "src/hooks/useMetaTags.ts"
      provides: "Dynamic document.title + meta description synced to i18n language"
      exports: ["useMetaTags"]
    - path: "src/locales/en/common.json"
      provides: "EN meta.title and meta.description strings"
      contains: "Kelly Battistoni"
    - path: "src/locales/es/common.json"
      provides: "ES meta.title and meta.description strings"
      contains: "Kelly Battistoni"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useMetaTags.ts"
      via: "useMetaTags() call"
      pattern: "useMetaTags"
    - from: "src/hooks/useMetaTags.ts"
      to: "src/locales/en/common.json"
      via: "t('meta.title'), t('meta.description')"
      pattern: "t\\('meta\\."
---

<objective>
Establish baseline Lighthouse scores, wire dynamic language-aware meta tags (title + description), add static OG tags and hreflang to index.html, and produce the OG card HTML/CSS preview artifact for user screenshot.

Purpose: SEO and social preview readiness — document.title and meta description respond to EN/ES language changes; OG/hreflang tags are static HTML so crawlers and social scrapers (LinkedIn, WhatsApp) can read them without executing JavaScript.

Output: useMetaTags hook, populated locale JSON strings, updated index.html with OG + hreflang tags, OG card HTML file for user screenshot.

Note on D-08: font-display:swap is a pre-condition already met per RESEARCH.md §Current Codebase State — the Google Fonts href already includes &display=swap. This plan verifies it remains unchanged (Task 3 acceptance criteria include a grep check); no implementation task is needed for D-08.
</objective>

<execution_context>
@C:\Users\KLBVT\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\KLBVT\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-performance/07-CONTEXT.md
@.planning/phases/07-polish-performance/07-RESEARCH.md
@.planning/phases/07-polish-performance/07-PATTERNS.md

<interfaces>
<!-- Existing hook pattern to replicate exactly — from src/hooks/useLocalizeDocumentAttributes.ts -->
<!-- useMetaTags follows this identical structure: useEffect on i18n.resolvedLanguage -->

src/hooks/useLocalizeDocumentAttributes.ts:
```typescript
export function useLocalizeDocumentAttributes(): void {
  const { i18n } = useTranslation()
  useEffect(() => {
    if (i18n.resolvedLanguage) {
      document.documentElement.lang = i18n.resolvedLanguage
    }
  }, [i18n.resolvedLanguage])
}
```

src/App.tsx — current hook call site (line 16):
```typescript
export default function App() {
  useLocalizeDocumentAttributes()
  const { isMobile } = useDeviceCapabilities()
  // ...
}
```

src/locales/en/common.json — current meta object (lines 7–10):
```json
"meta": {
  "title": "",
  "description": ""
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighthouse Baseline Audit</name>
  <files>No files modified — audit only</files>
  <read_first>
    - .planning/phases/07-polish-performance/07-RESEARCH.md §Current Codebase State — read the verified bundle baseline (index.html exact state, locale meta structure, confirmed absence of OG tags) to understand what the build contains before running Lighthouse. This section confirms the current single-bundle size (~510KB) and the font-display:swap pre-condition.
  </read_first>
  <action>
    Run the production build and serve it, then execute a Lighthouse mobile audit to establish baseline scores before any changes:

    1. Run: npm run build
    2. In a separate terminal, run: npx vite preview --port 4173
    3. Open Chrome DevTools → Lighthouse panel → set device to "Mobile" → run audit against http://localhost:4173
    4. Record the four category scores: Performance, Accessibility, Best Practices, SEO
    5. Note the LCP value from the Performance diagnostics panel
    6. Stop the vite preview server

    The baseline scores are for information only — no pass/fail threshold here. Document them in the SUMMARY when the plan completes. They will be used in Plan 07-04 for before/after comparison.

    Do NOT run Lighthouse against the Vite dev server (npm run dev). Always use the production build via vite preview, per RESEARCH.md Pitfall 3.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -5</automated>
    <!-- Lighthouse scores are produced by a human-operated browser tool, not a CLI command. -->
    <!-- After build succeeds, the executor opens Chrome DevTools Lighthouse, runs the mobile audit, -->
    <!-- and records the four scores manually. Those scores are documented in 07-01-SUMMARY.md -->
    <!-- under "Baseline Scores" and serve as the comparison baseline for Plan 07-04. -->
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0 and produces dist/assets/index-*.js
    - Vite build output shows the current bundle size (expected ~510KB before lazy split)
    - Four Lighthouse scores are recorded: Performance, Accessibility, Best Practices, SEO
    - Baseline scores are noted in the plan SUMMARY (written at plan completion) for comparison in Plan 07-04
  </acceptance_criteria>
  <done>Build succeeds; baseline Lighthouse scores documented in SUMMARY under "Baseline Scores"</done>
</task>

<task type="auto">
  <name>Task 2: Wire useMetaTags hook + populate locale strings</name>
  <files>src/hooks/useMetaTags.ts, src/locales/en/common.json, src/locales/es/common.json, src/App.tsx</files>
  <read_first>
    - src/hooks/useLocalizeDocumentAttributes.ts — exact pattern to replicate (already in interfaces block above)
    - src/locales/en/common.json — current meta structure with empty strings
    - src/locales/es/common.json — current meta structure with empty strings
    - src/App.tsx — hook call site; add useMetaTags() after useLocalizeDocumentAttributes() on line 17
  </read_first>
  <action>
    Step 1 — Create src/hooks/useMetaTags.ts following the exact pattern of useLocalizeDocumentAttributes.ts (per D-01):
    - Import useEffect from 'react' and useTranslation from 'react-i18next'
    - Export function useMetaTags(): void
    - Call useTranslation('common') to get { t, i18n }
    - Inside useEffect: read title = t('meta.title') and description = t('meta.description')
    - If title is truthy, assign to document.title
    - If description is truthy, query document.querySelector with type parameter HTMLMetaElement for 'meta[name="description"]' and call setAttribute('content', description) on it
    - Dependency array: [i18n.resolvedLanguage, t]

    Step 2 — Populate src/locales/en/common.json meta object (per D-02):
    - meta.title: "Kelly Battistoni — AI Automations Manager & Systems Thinker" (59 chars)
    - meta.description: "AI Automations Manager building end-to-end workflow strategies across Make.com, n8n, Claude, and Supabase for global clients. Systems thinker. Business-first." (156 chars)

    Step 3 — Populate src/locales/es/common.json meta object (per D-02):
    - meta.title: "Kelly Battistoni — Gerente de Automatizaciones IA" (49 chars)
    - meta.description: "Gerente de automatizaciones IA con visión de sistemas. Construyo estrategias de automatización de extremo a extremo para clientes globales con Make.com, n8n y Claude." (167 chars — user may trim at checkpoint)

    Step 4 — Add useMetaTags to src/App.tsx:
    - Add import statement: import { useMetaTags } from '@/hooks/useMetaTags'
    - Call useMetaTags() on the line immediately after useLocalizeDocumentAttributes() (currently line 16)

    Do NOT use react-helmet-async or any external library — direct DOM mutation via useEffect per D-01.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useMetaTags.ts exists and exports function useMetaTags(): void
    - useMetaTags.ts contains useEffect with dependency array [i18n.resolvedLanguage, t]
    - useMetaTags.ts contains document.querySelector with 'meta[name="description"]' selector
    - src/locales/en/common.json meta.title equals "Kelly Battistoni — AI Automations Manager & Systems Thinker"
    - src/locales/es/common.json meta.title equals "Kelly Battistoni — Gerente de Automatizaciones IA"
    - src/App.tsx contains both useLocalizeDocumentAttributes() and useMetaTags() calls
    - npx tsc -b --noEmit exits with code 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>useMetaTags hook created and wired; locale strings populated; TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 3: Add OG meta tags + hreflang to index.html; produce OG card artifact</name>
  <files>index.html, public/og-card-preview.html</files>
  <read_first>
    - index.html — current state: static title "Kelly Battistoni — Portfolio", existing meta[name="description"], Google Fonts link with &display=swap already present (no change needed per D-08)
  </read_first>
  <action>
    Step 1 — Edit index.html to add OG meta tags and hreflang links immediately after the existing meta[name="description"] line (line 7), before the Google Fonts preconnect:

    Open Graph tags to add (per D-03, D-04):
    - og:type with content "website"
    - og:url with content "https://kellybattistoni.github.io/"
    - og:title with content "Kelly Battistoni — AI Automations Manager & Systems Thinker"
    - og:description with content "Systems thinker and full-stack AI automations manager building end-to-end automation strategies across Make.com, n8n, Claude, and Supabase."
    - og:image with content "https://kellybattistoni.github.io/og-image.png"
    - og:image:width with content "1200"
    - og:image:height with content "630"
    - twitter:card with content "summary_large_image"

    hreflang links to add immediately after the OG block (per D-05):
    - link rel="alternate" hreflang="en" href="https://kellybattistoni.github.io/"
    - link rel="alternate" hreflang="es" href="https://kellybattistoni.github.io/"
    - link rel="alternate" hreflang="x-default" href="https://kellybattistoni.github.io/"

    Do NOT touch the Google Fonts link — &display=swap is already present per D-08.

    Step 2 — Create public/og-card-preview.html as a standalone HTML file that renders the OG card at 1200×630 for user screenshot (per D-03):
    - Full-bleed div sized to exactly 1200×630px
    - Background: #050505
    - Top-left area (80px from left, 80px from top): thin #FF4500 horizontal rule, 80px wide, 1px tall
    - Name: "Kelly Battistoni" — Playfair Display 72px weight 700 white, positioned at left 80px, top 180px
    - Role: "AI Automations Manager" — Inter 28px weight 400 rgba(255,255,255,0.65), positioned at left 80px, top 285px
    - Tagline: "AI Strategy · Process Design · Full-Stack Automation" — Inter 18px weight 300 rgba(255,255,255,0.38), positioned at left 80px, top 345px
    - URL credit: "kellybattistoni.github.io" — Inter 14px weight 300 rgba(255,255,255,0.3), positioned at bottom-right with 40px margin
    - Embed Google Fonts via link tag (Playfair Display 700 + Inter 300/400) so the card renders correctly when opened in a browser
    - The file is a self-contained preview artifact only — it is not served as part of the site
    - No references to src/ directories or node_modules/ — all styles must be inline or via CDN link tags
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&amp;1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains meta property="og:type" with content="website"
    - index.html contains meta property="og:image" with content="https://kellybattistoni.github.io/og-image.png"
    - index.html contains meta property="og:image:width" with content="1200"
    - index.html contains meta property="og:image:height" with content="630"
    - index.html contains meta name="twitter:card" with content="summary_large_image"
    - index.html contains link rel="alternate" hreflang="en" href="https://kellybattistoni.github.io/"
    - index.html contains link rel="alternate" hreflang="es" href="https://kellybattistoni.github.io/"
    - index.html contains link rel="alternate" hreflang="x-default" href="https://kellybattistoni.github.io/"
    - Google Fonts href in index.html still contains &display=swap (unchanged per D-08)
    - public/og-card-preview.html exists as a standalone file
    - public/og-card-preview.html contains a link tag referencing Playfair Display from Google Fonts
    - public/og-card-preview.html contains inline CSS with #050505 background-color and #FF4500 accent
    - public/og-card-preview.html renders without external dependencies (no src/ or node_modules/ references)
    - public/og-card-preview.html contains "Kelly Battistoni" text in a heading element
    - npx tsc -b --noEmit exits with code 0
  </acceptance_criteria>
  <done>OG tags and hreflang in index.html; OG card preview HTML artifact created in public/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - useMetaTags hook wired into App.tsx — updates document.title and meta description on language change
    - EN and ES meta title + description populated in locale JSON files (Claude's drafts)
    - OG meta tags and hreflang links added to index.html (static, crawler-readable)
    - OG card preview HTML file created at public/og-card-preview.html
  </what-built>
  <how-to-verify>
    1. Run: npm run dev

    2. Open http://localhost:5173 in Chrome

    3. Check dynamic meta tags:
       - Open DevTools → Console, run: document.title
       - Expected (EN): "Kelly Battistoni — AI Automations Manager & Systems Thinker"
       - Toggle language to ES using the switcher
       - Run document.title again — expected: "Kelly Battistoni — Gerente de Automatizaciones IA"

    4. Check meta description updates:
       - In Console: document.querySelector('meta[name="description"]').getAttribute('content')
       - Expected (ES): the Spanish description string
       - Toggle back to EN, repeat — expected: the English description string

    5. Review the meta copy drafts — do the EN and ES titles and descriptions match your voice and brand? Edit the locale JSON files directly if you want to refine wording.

    6. REQUIRED — Create public/og-image.png now:
       Open public/og-card-preview.html in Chrome. The card should render 1200×630px dark background with "Kelly Battistoni" in Playfair Display and role/tagline in Inter. Screenshot this page at exactly 1200×630px and save the screenshot as public/og-image.png. This file is checked in Plan 07-04 — it must exist before proceeding to Plan 07-02.

    7. Check OG tags in index.html source (Cmd/Ctrl+U): confirm og:image, hreflang tags are present.
  </how-to-verify>
  <resume-signal>Type "approved" when meta copy is acceptable and you have saved public/og-image.png to the public/ directory. public/og-image.png is REQUIRED before proceeding — do not skip this step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → Social crawlers | OG/hreflang in index.html is consumed by LinkedIn/WhatsApp/Googlebot with no auth |
| i18next → document DOM | useMetaTags writes to document.title and meta[name="description"] — values come from trusted locale JSON files, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | og:image URL in index.html | accept | Static GitHub Pages hosting; no server-side injection surface |
| T-07-02 | Information Disclosure | Meta description content | accept | Content is intentionally public-facing portfolio copy; no PII |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; @playwright/test added in Plan 07-04 |
</threat_model>

<verification>
After task completion, verify the full plan:

1. npm run build exits with code 0 — no TypeScript or bundler errors
2. document.title and meta[name="description"] update correctly on language toggle (confirmed via checkpoint)
3. index.html source contains all 8 OG meta tags and 3 hreflang links
4. public/og-card-preview.html renders the OG card correctly in a browser
5. public/og-image.png exists (created at checkpoint — required before Plan 07-02)
</verification>

<success_criteria>
- Lighthouse baseline scores recorded (all four categories)
- document.title and meta description respond to EN/ES language changes
- index.html static OG tags present and correct for LinkedIn/WhatsApp scrapers
- index.html hreflang links present for Google Search Console (D-05)
- User has reviewed and approved (or refined) the meta copy drafts
- OG card preview HTML artifact exists for screenshot workflow
- public/og-image.png saved by user at checkpoint (REQUIRED)
</success_criteria>

<output>
Create .planning/phases/07-polish-performance/07-01-SUMMARY.md when done.
Include the baseline Lighthouse scores in the summary under a "Baseline Scores" section.
</output>

---
phase: 08-deployment
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - README.md
autonomous: true
requirements: [INFRA-05]

must_haves:
  truths:
    - "A new contributor (or future-Kelly) can read README.md and understand how to deploy without reading any source files"
    - "The one-time GitHub Pages Settings step (select gh-pages branch) is documented so it can be performed after the first CI run"
    - "Local dev commands (install, dev, build, lint, e2e) are listed so the README doubles as project onboarding"
  artifacts:
    - path: "README.md"
      provides: "Project overview + deployment runbook"
      contains: "## Deployment"
      min_lines: 40
  key_links:
    - from: "README.md"
      to: ".github/workflows/deploy.yml"
      via: "Deployment section references CI/CD workflow"
      pattern: "\\.github/workflows/deploy\\.yml"
    - from: "README.md"
      to: "Settings > Pages one-time step"
      via: "Post-first-deploy checklist"
      pattern: "Settings.*Pages"
---

<objective>
Create a project README that doubles as the deployment runbook so the CI/CD flow, one-time GitHub Pages config step, and local dev commands are all documented in one canonical place.

Purpose: ROADMAP §Phase 8 success criterion #4 calls for "a documented manual ... flow ... in the README". D-01 supersedes the manual flow with CI, but the README still needs to document the CI pipeline, the post-first-deploy Settings step (research Pitfall 2), and the standard project onboarding commands.

Output: A single `README.md` at the repo root, structured for both a brand-new visitor and a returning maintainer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-deployment/08-CONTEXT.md
@.planning/phases/08-deployment/08-RESEARCH.md
@package.json
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — project overview + deployment runbook</name>
  <files>README.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/phases/08-deployment/08-CONTEXT.md
    .planning/phases/08-deployment/08-RESEARCH.md
    package.json
    .github/workflows/deploy.yml
  </read_first>
  <action>
    Create `README.md` at the repo root. No README currently exists, so this is a from-scratch file. Use plain GitHub-flavored Markdown, no badges, no shields.io decorations, no emoji.

    Required sections (in this order, with these exact H2 headings so the verification grep can confirm structure):

    1. `# Kelly Battistoni — Portfolio` (H1, em-dash)
    2. A one-paragraph project summary pulled from `.planning/PROJECT.md` core value: bilingual EN/ES dark-cinematic portfolio, GitHub Pages deployment target `https://KellyBattistoni.github.io/`.
    3. `## Tech Stack` — bullet list pulled from package.json: React 19, Vite 8, TypeScript 6, Tailwind v4, GSAP 3.15 + @gsap/react, react-i18next 17, OGL 1, Playwright (WebKit). Mention Node 22 LTS as the CI runtime.
    4. `## Local Development` — fenced bash block with: `npm ci`, `npm run dev` (Vite dev server), `npm run build` (tsc + vite build), `npm run lint` (eslint), `npm run preview` (preview prod build), and the e2e command pair: `npx vite preview < /dev/null &` then `npx wait-on http://localhost:4173 && npx playwright test`. Note that on Windows PowerShell the stdin redirect syntax differs (point to Pitfall 1 of research for the workaround).
    5. `## Deployment` — describe the CI/CD pipeline:
       - Trigger: every push to `main` runs `.github/workflows/deploy.yml`
       - Pipeline order: checkout → setup Node 22 → `npm ci` → install Playwright WebKit → `npm run build` → `npm run lint` → start vite preview (background, stdin redirected) → wait-on → playwright test → peaceiris/actions-gh-pages@v4 publishes `dist/` to `gh-pages` branch with `force_orphan: true`
       - Concurrency: overlapping pushes are queued (not cancelled)
       - No manual `npm run deploy` — CI is the only deploy path (cite D-01)
    6. `## One-Time GitHub Pages Setup` — exact steps after the FIRST successful CI run (research Pitfall 2 — user sites default to `main`, must be manually switched to `gh-pages`):
       1. Wait for the first run of `.github/workflows/deploy.yml` to complete successfully (green check on Actions tab).
       2. Confirm a `gh-pages` branch exists in the repo branch list.
       3. Go to `Settings > Pages > Build and deployment`.
       4. Source: `Deploy from a branch`.
       5. Branch: select `gh-pages`, folder `/ (root)`, click Save.
       6. Wait 1–2 minutes for the first publication. Site is then live at `https://KellyBattistoni.github.io/`.
    7. `## 404 Handling` — explain `public/404.html` is an instant meta-refresh redirect to `/`; GitHub Pages auto-serves it for any unknown path; HashRouter-style `#/` deep links are handled in-browser and bypass the 404 path entirely.
    8. `## Project Structure` — one short paragraph or 6–10 bullet tree pointing at: `src/components/` (UI), `src/locales/{en,es}/` (i18n JSON), `src/lib/` (i18n + gsap modules), `public/` (static assets including 404.html, og-image, CV PDFs, fonts), `e2e/` (Playwright smoke test), `.planning/` (GSD framework — phases, roadmap, state).
    9. `## License` — short note: "All rights reserved. Personal portfolio of Kelly Battistoni." (No OSS license — this is a personal site, not a reusable library.)

    Do NOT include: emoji, GitHub Actions badges, "Made with ❤️" footers, contribution guidelines (this is not an open source project), demo screenshots (they go stale and the live URL is documented in §Deployment), Lighthouse score boasts (covered in Phase 7 SUMMARY).

    Length target: 80–140 lines of Markdown. Concrete, not promotional.
  </action>
  <verify>
    <automated>test -f README.md && grep -E "^# Kelly Battistoni" README.md && grep -E "^## Tech Stack" README.md && grep -E "^## Local Development" README.md && grep -E "^## Deployment" README.md && grep -E "^## One-Time GitHub Pages Setup" README.md && grep -E "^## 404 Handling" README.md && grep -F "peaceiris/actions-gh-pages" README.md && grep -F "gh-pages" README.md && grep -F "Settings > Pages" README.md && grep -F "< /dev/null" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root
    - File contains H1 starting with `# Kelly Battistoni` (em-dash U+2014 in title)
    - File contains all six required H2 sections: `## Tech Stack`, `## Local Development`, `## Deployment`, `## One-Time GitHub Pages Setup`, `## 404 Handling`, `## Project Structure`, `## License` (verify all H2 anchors via grep `^## `)
    - File mentions `.github/workflows/deploy.yml` by literal path
    - File mentions `peaceiris/actions-gh-pages` by name
    - File mentions `gh-pages` branch
    - File documents the `Settings > Pages` one-time step
    - File documents the `< /dev/null` stdin redirect (or its Windows equivalent)
    - File is between 60 and 200 lines (no padding, no over-engineering)
    - No emoji characters (regex `[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]` matches nothing)
    - No badge markdown (no `![]` shields.io patterns)
  </acceptance_criteria>
  <done>README.md is the canonical entry point — a new reader understands what the project is, how to run it locally, how it deploys, and what to do after the first CI run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ implementation | README claims must match `.github/workflows/deploy.yml` reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | docs leaking secrets | accept | README documents `${{ secrets.GITHUB_TOKEN }}` as a literal — this is the auto-injected token name from GitHub Actions, not a secret value; no risk |
| T-08-12 | Tampering | docs drifting from workflow | mitigate | Plan 04 human checkpoint includes "run a deploy and verify the docs match observed behavior" — drift caught before it ships |
</threat_model>

<verification>
- README.md exists, has all required H2 sections, mentions deploy.yml + peaceiris + gh-pages + Settings > Pages + stdin redirect.
</verification>

<success_criteria>
- [ ] README.md exists at repo root
- [ ] All seven H2 sections present (Tech Stack, Local Development, Deployment, One-Time GitHub Pages Setup, 404 Handling, Project Structure, License)
- [ ] CI/CD pipeline accurately described (matches deploy.yml step order)
- [ ] One-time `Settings > Pages` step documented
- [ ] No emoji, no badges, no padding
</success_criteria>

<output>
Create `.planning/phases/08-deployment/08-03-SUMMARY.md` when done. Record: final README line count, list of H2 sections in order, and any divergence between documented and actual workflow steps (should be zero).
</output>
